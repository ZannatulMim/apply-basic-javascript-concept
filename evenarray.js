// function evennumbers(numbers) {
//     for (let i = 0; i < numbers.length; i++) {
//         const index = i;
//         const element = numbers[index];
//         if (element % 2 === 0) {
//             console.log(index, elemrnt);
//         }
//     }

// }




// function evennumber(numbers) {
//     for (let i = 0; i < numbers.length; i++) {
//         const index = i;
//         const element = numbers[index];
//         if (element % 2 === 0) {
//             console.log(index, element);
//         }
//     }
// }


const myNumbers = [12, 4, 5, 6, 7, 4, 3, 5];
evennumber(myNumbers);



// function evennumber(numbers) {
//     for (let i = 0; i < numbers.length; i++) {
//         const index = i;
//         const element = numbers[index];
//         if (element % 2 === 0) {
//             console.log(index, element);
//         }
//     }
// }





function evennumber(numbers) {
    for (let i = 0; i < numbers.length; i++) {
        const index = i;
        const element = numbers[index];
        if (element % 2 == 0) {
            console.log(index, element);
        }
    }
}
